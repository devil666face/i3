# need to install
# apt-get install i3-wm lxappearance picom dunst nitrogen gnome-screensaver qt5-style-kvantum qt5ct flameshot blueman volumeicon-alsa xfce4-power-manager dmenu gnome-settings-daemon pavucontrol

# mod4 = Win
set $mod Mod4

# font
font pango:JetBrains Mono Nerd Font SemiBold 14

# set screen settings
exec_always --no-startup-id xrandr --auto
# exec_always --no-startup-id xrandr --output HDMI-0 --mode 1366x768 --pos 0x0 --rotate normal --output eDP-1-0 --primary --mode 1920x1080 --pos 1366x0 --rotate normal

# start i3
exec --no-startup-id dex --autostart --environment i3

# Applets: network; lock-screen; volume in bar; power and brightness; bluetooth
exec_always --no-startup-id killall nm-applet; nm-applet
exec_always --no-startup-id killall gnome-screensaver; gnome-screensaver
exec_always --no-startup-id killall volumeicon; volumeicon
exec_always --no-startup-id killall xfce4-power-manager; xfce4-power-manager
exec_always --no-startup-id killall blueman-applet; blueman-applet

# tap on touchpad
exec_always --no-startup-id xinput set-prop "SynPS/2 Synaptics TouchPad" "libinput Tapping Enabled" 1

# optional gnome-settings-daemons
exec_always --no-startup-id /usr/libexec/gsd-xsettings
exec_always --no-startup-id /usr/libexec/gsd-a11y-settings
exec_always --no-startup-id /usr/libexec/gsd-disk-utility-notify
exec_always --no-startup-id /usr/libexec/gsd-usb-protection
exec_always --no-startup-id /usr/libexec/gsd-wwan

# Optional check this daemons
# ps -eH  | grep system76
#     562 ?        00:00:00   system76-power
#     563 ?        00:00:00   system76-schedu
#    1432 ?        00:00:00     system76-schedu

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty --config-file ~/.config/i3/alacritty.yml

# kill focused window
bindsym $mod+q kill

# filebrowser
bindsym $mod+f exec --no-startup-id nautilus -w

# lock screen
bindsym $mod+l exec --no-startup-id gnome-screensaver-command -l

# settings
bindsym $mod+s exec --no-startup-id gnome-control-center

# browser
bindsym $mod+w exec --no-startup-id mercury-browser

# telegram
bindsym $mod+t exec --no-startup-id Updater

# obsidian
bindsym $mod+o exec --no-startup-id obsidian

# keepass
bindsym $mod+k exec --no-startup-id keepassxc

# screenshot
bindsym Print exec --no-startup-id flameshot gui

# dmenu for start apps
bindsym  $mod+d exec --no-startup-id i3-dmenu-desktop --dmenu="dmenu -nb '#1d2021' -sf '#1d2021' -sb '#98971a' -nf '#d79921' -fn 'JetBrains Mono Nerd Font:size=14:style:Bold:antialias=true:autohint=true'"

# choose window
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move window
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
# bindsym $mod+d focus child

# workspaces
set $ws1 "1:1 💡"
set $ws2 "2:2 🌐"
set $ws3 "3:3 📨"
set $ws4 "4:4 🧠"
set $ws5 "5:5 💻"
set $ws6 "6:6 🔐"
set $ws7 "7:7 📦"
set $ws8 "8:8 🔧"
set $ws9 "9:9 📺"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9

# output workspace on monitor
workspace $ws1 output eDP-1
workspace $ws2 output eDP-1
workspace $ws3 output eDP-1
workspace $ws4 output eDP-1
workspace $ws5 output eDP-1
workspace $ws6 output eDP-1
workspace $ws7 output eDP-1
workspace $ws8 output eDP-1
workspace $ws9 output HDMI-0

# rules for apps and ws
assign [class="(?i)firefox"] $ws2
assign [class="mercury-default"] $ws2
assign [class="Thorium-browser"] $ws2
assign [class="TelegramDesktop"] $ws3
assign [class="obsidian"] $ws4
assign [class="org.remmina.Remmina"] $ws5
assign [class="KeePassXC"] $ws6
assign [class="Virt-manager"] $ws7
assign [class="Gnome-control-center"] $ws8

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Close i3 and go to X session' -B 'Yes' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# active window hint
default_border pixel 1
default_floating_border pixel 1
new_window pixel 3
hide_edge_borders smart

# pallet gruvbox
set $bg #282828
set $fg #ebdbb2
set $red #cc241d
set $green #98971a
set $yellow #d79921
set $blue #458588
set $purple #b16286
set $aqua #689d68
set $gray #a89984
set $darkgray #1d2021

# class                 border    backgr    text      indicator child_border
client.focused          $green    $green    $darkgray $purple   $yellow 
client.focused_inactive $darkgray $darkgray $yellow   $purple   $gray
client.unfocused        $darkgray $darkgray $yellow   $purple   $gray
client.urgent           $red      $red      $white    $red      $red


bar {
    position top
    output eDP-1
    strip_workspace_numbers yes
    tray_output primary
    tray_padding 4
    font pango:JetBrains Mono Nerd Font Bold 12
    separator_symbol " "
    status_command i3status --config ~/.config/i3/i3status.conf

#                          border 	 bg		   txt		 indicator
    colors {
        separator          $ebdbb2
        background         $darkgray 
        statusline         $fg
    	focused_workspace  $green    $green    $darkgray $purple   
    	active_workspace   $darkgray $darkgray $yellow   $purple   
    	inactive_workspace $darkgray $darkgray $yellow   $purple   
    	urgent_workspace   $red      $red      $white    $red      
    }
}

# language toggle
exec --no-startup-id setxkbmap -layout us,ru -option 'grp:lalt_lshift_toggle'
# wallpaper
exec --no-startup-id nitrogen --restore
# picom compositor for windows
exec_always --no-startup-id sleep 5 && picom --experimental-backend --config /home/pps/.config/i3/picom.conf
# alert daemon
exec_always --no-startup-id killall dunst; dunst -conf $HOME/.config/i3/dunstrc

# optional
# exec_always --no-startup-id syndaemon -i 0.8 -d -k
# for_window [class="^.*"] border pixel 0
# optional if your not use xfce4-power-manager
# bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 10
# bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 10
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
